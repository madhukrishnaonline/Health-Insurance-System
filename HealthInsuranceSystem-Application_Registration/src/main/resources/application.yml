#Configure Properties from Config Server
spring:
  config:
    import: optional:configserver:http://localhost:9999
  profiles:
    active: default
  application:
    name: HealthInsurance-Application-Registration

#server port
server:
  port: 2020
